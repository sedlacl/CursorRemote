# Keep installs on public npm regardless of corporate ~/.npmrc (Nexus / Plus4U).
# Otherwise package-lock "resolved" URLs can point at private registries and break CI.
ignore-scripts=true
registry=https://registry.npmjs.org/
replace-registry-host=always
